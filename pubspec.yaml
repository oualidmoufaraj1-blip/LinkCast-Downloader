name: fc_app3_downloader_aftvnews
description: "LinkCast Downloader — download files, browse links, and cast to TV on iOS."
publish_to: 'none'

version: 1.0.0+2

environment:
  sdk: ^3.11.0

dependencies:
  flutter:
    sdk: flutter
  cupertino_icons: ^1.0.8
  shared_preferences: ^2.5.3
  path_provider: ^2.1.5
  webview_flutter: ^4.13.0
  http: ^1.4.0
  intl: ^0.20.2
  share_plus: ^11.0.0
  mime: ^2.0.0
  package_info_plus: ^8.3.0
  url_launcher: ^6.3.1
  better_native_video_player: ^1.3.1
  just_audio: ^0.10.6
  flutter_to_airplay: ^2.1.0

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^6.0.0

flutter:
  uses-material-design: false
